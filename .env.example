# .env.example
PORT=3000
DATABASE_URL=postgresql://postgres:tu_password@db:5432/tienda_viajes
JWT_SECRET=tu_secreto_jwt

# Configuracion de Email (Gmail)
GMAIL_USER=tu_correo@gmail.com
GMAIL_PASS=tu_contraseña_de_aplicacion_gmail
# URL del Frontend (usada en los correos para enlaces de verificación/reseteo)
FRONTEND_URL=http://localhost:8080
# Si tu frontend corre en un puerto diferente o dominio en producción, ajústalo.
# Por ejemplo, si usas `live-server` o similar para `frontend/html` en el puerto 8080.
# O si el frontend está desplegado en un dominio diferente.
